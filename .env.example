# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real tokens.

# Fine-grained PAT scoped to this repo with Issues: R/W, Contents: R/W, Pull requests: R/W.
# Used by /plan-sync for local runs and by workflows when FACTORY_PAT secret is set.
GITHUB_PAT=
