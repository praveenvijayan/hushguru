# Codex skill metadata. Explicit-only: these skills have side effects, so Codex
# must not invoke them implicitly. Invoke with /skills or $plan-sync.
policy:
  allow_implicit_invocation: false
interface:
  display_name: "plan-sync"
  short_description: "Compile plan/*.md into GitHub issues now."
