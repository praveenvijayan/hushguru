# Codex skill metadata. Explicit-only: editing MEMORY.md is a side effect, so
# Codex must not invoke it implicitly. Invoke with /skills or $memory-compact.
policy:
  allow_implicit_invocation: false
interface:
  display_name: "memory-compact"
  short_description: "Prune and dedupe MEMORY.md; verify links."
